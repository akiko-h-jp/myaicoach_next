# Supabase設定
# Supabaseダッシュボードの「Settings」→「Database」→「Connection string」→「URI」から取得
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?sslmode=require"

# Supabaseダッシュボードの「Settings」→「API」から取得
SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"
SUPABASE_ANON_KEY="[YOUR-ANON-KEY]"
SUPABASE_SERVICE_ROLE_KEY="[YOUR-SERVICE-ROLE-KEY]"

# ブラウザ側で使用するSupabase設定（NEXT_PUBLIC_で始まるものはブラウザに公開されます）
NEXT_PUBLIC_SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[YOUR-ANON-KEY]"

# NextAuth設定
# 本番環境では、実際のドメインに変更してください（例: https://your-app.vercel.app）
NEXTAUTH_URL="http://localhost:3000"

# 任意の長いランダムな文字列（以下のコマンドで生成可能: openssl rand -base64 32）
NEXTAUTH_SECRET="[YOUR-RANDOM-SECRET-KEY]"

# OpenAI API設定（オプション - AI機能を使用する場合のみ必要）
# OpenAI Platform (https://platform.openai.com/api-keys) で取得
OPENAI_API_KEY=""

# LINE Messaging API設定（オプション - LINE通知を使用する場合のみ必要）
# LINE Developers (https://developers.line.biz/) で取得
LINE_CHANNEL_ACCESS_TOKEN=""
LINE_USER_ID=""
LINE_PUSH_ENDPOINT="https://api.line.me/v2/bot/message/push"
